let images = [...document.querySelectorAll('.flow')];

let active = document.querySelector('[active]');
active.style.display = 'block';

// console.log(active.style.top);
setInterval(() => {
    
}, 1000);